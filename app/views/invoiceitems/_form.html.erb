<%= form_for(@invoiceitem) do |f| %>
  <% if @invoiceitem.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@invoiceitem.errors.count, "error") %> prohibited this invoiceitem from being saved:</h2>

      <ul>
      <% @invoiceitem.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :invoice_id %><br />
    <%= f.number_field :invoice_id %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :amount %><br />
    <%= f.number_field :amount %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
